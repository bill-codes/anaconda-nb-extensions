This is the Customized Wakari IPython directory.

Setup:

If you ipython-dir is `/path/to/ipython-dir`, then

1.  In /path/to/ipython-dir/profile_wakari/ipython_notebook_config.py, 
    in line 102 you have to add the dir of the nbextensions folder:

    c.NotebookApp.nbextensions_path = [u'/path/to/ipython-dir/nbextensions']

2.  In `/path/to/ipython-dir/profile_wakari/static/custom/custom.js`,
    in line 9 you have to add the dir of the sync.py script:

    sync.parameters('/path/to/ipython-dir/nbextensions/syncer');

3.  `/path/to/ipython-dir/profile_wakari/default_kernel` is a file containing
    in the first line the name of the default kernel we want the notebook to start with.

Run:

    ipython notebook --ipython-dir=/path/to/ipython-dir --profile=wakari
